apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteUDP
metadata:
  name: dns
spec:
  entryPoints:                 
    - dns-udp
  routes:                       
  - match: HostSNI(`*`)         
    services:                  
    - name: homelab-dns         
      port: 53               

